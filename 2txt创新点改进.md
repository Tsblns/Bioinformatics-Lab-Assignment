提示词：
我选，情况B，甲基化的维度太高也没事，降维后再用。现在你按实际情况分成几个窗口，每个窗口给我一段提示词，确保我能完成每个阶段的任务。我们小组8个人，最好是搞清楚分工

回答：
可以，按你选的 **情况 B（六组学都进主流程）**，我建议你们 **分成 8 个窗口、8 个人**，这样最顺，也最符合课程要求。因为题目本身就是围绕突变、CNV、甲基化、RNA、miRNA、表型和随访去做“鲁棒且有临床意义”的胃癌分型，方法也明确允许无监督聚类、随机森林、SVM 等，而且报告里要写清楚分工和贡献度。 生存表里也给了 OS、DSS、DFI、PFI 四类终点，所以后面单独开一个窗口做生存验证是完全对路的。

先说你们当前这批文件的**实际情况**，这是我按文件真实读出来后给的分工依据：

* 六组学共同可用的原发肿瘤样本，大约 **288** 个
* 四组学 `RNA + miRNA + CNV + RPPA` 约 **290** 个
* miRNA 缺失很多，不能粗暴直接填满后使用
* RNA、CNV 基本完整
* RPPA 缺失不算严重
* 你现在上传到对话里的 **甲基化文件只有 6 个探针**，**突变文件只有 29 个基因**，都是“部分数据”
* 所以最稳的策略不是停下来等，而是：**先把六组学的可替换流程搭好并跑通；以后如果拿到全量甲基化/全量突变，只替换对应窗口重新跑**

---

## 先定统一规则

这几条你们 8 个人都要统一：

1. **只保留原发肿瘤样本 `-01`**
2. **先统一共同样本，再做缺失处理**
3. **聚类阶段绝不能用 clinical/survival**
4. **clinical 和 survival 只能后验验证**
5. **缺失处理顺序：删高缺失特征 → 再填剩余缺失**
6. **所有人都使用窗口1导出的共同样本列表**
7. **所有代码固定随机种子**
8. **所有中间结果都保存成 csv/xlsx/png，方便最后汇总**

---

## 推荐的 8 人分工

### 1号：组长 / 总控 / 样本对齐

负责所有数据清点、统一样本、给后面所有人发标准输入。

### 2号：RNA + miRNA 预处理

负责表达层。

### 3号：CNV + 突变预处理

负责遗传变异层。

### 4号：甲基化预处理

负责表观层，也是最容易出问题的一层。

### 5号：多组学整合 + 聚类

负责融合所有组学低维表示并出最终 cluster。

### 6号：鲁棒性评估 + 亚型特征图

负责说明“为什么这个聚类不是拍脑袋”。

### 7号：临床 + 生存验证

负责“临床意义”。

### 8号：分类模型 + 报告/PPT 汇总

负责最终“系统雏形”和成稿整合。

---

## 执行顺序

按这个顺序来最稳：

**窗口1先做**
然后 **窗口2、3、4并行**
接着 **窗口5**
然后 **窗口6、7并行**
最后 **窗口8收尾**

---

# 窗口1：数据总控与共同样本表

给 1 号同学

```text
现在只做“多组学项目总控与样本对齐”，不要提前做聚类和建模。

我当前项目是：基于 TCGA-STAD 六组学（突变、CNV、甲基化、RNA、miRNA、RPPA）建立鲁棒且有临床意义的胃癌分型系统。当前文件里甲基化和突变是部分数据，但我要按“完整六组学可替换流程”搭建。

请你基于我上传的这些文件，严格一步一步完成以下任务，并给出可直接运行的 Python 代码（pandas/numpy 为主）：

1. 读取每个文件，列出每个数据集的维度、样本数、特征数、样本 ID 格式。
2. 只保留原发肿瘤样本（sample type = 01），去掉正常样本（11）。
3. 统一六组学样本 ID 格式，明确是保留完整 sample barcode，还是同时生成 patient barcode。
4. 求六组学共同样本交集，输出一个 master_samples_6omics.csv。
5. 另外输出 clinical 和 survival 中与六组学共同样本重叠的样本表。
6. 给出每个数据集的缺失概况：总体缺失率、每个特征缺失率、每个样本缺失率。
7. 给出后续预处理的统一规则建议：先删哪些特征、哪些组学适合什么标准化、哪些不能乱标准化。
8. 输出一个“后续窗口统一输入说明”，告诉其他窗口应该读取哪个文件、使用哪一列样本 ID。
9. 所有结果保存成 csv/xlsx，并给出简洁解释。

要求：
- 不要跳步
- 先解释为什么“先统一共同样本，再处理缺失”
- 代码要完整、可运行、带注释
- 最终明确告诉我：六组学共同样本数是多少、clinical/survival 可用于后验验证的样本数是多少
- 给出推荐的文件命名规范
```

### 这个窗口的产出

* `master_samples_6omics.csv`
* `clinical_survival_overlap.csv`
* `dataset_qc_summary.xlsx`
* `preprocess_rules.md`

---

# 窗口2：RNA + miRNA 预处理

给 2 号同学

```text
现在只做 RNA 和 miRNA 的预处理，不做聚类、不做临床分析。

背景：
- 我们已经有窗口1导出的六组学共同样本列表 master_samples_6omics.csv
- 目标是给后续多组学整合聚类准备高质量、低噪声、可降维的 RNA 和 miRNA 特征矩阵
- 当前 miRNA 缺失较多，请你平衡“处理难度”和“结果可靠性”，不要简单粗暴全填充

请你基于我上传的 RNA 和 miRNA 文件，以及 master_samples_6omics.csv，完成以下任务，并给出完整 Python 代码（pandas/sklearn）：

1. 读取 RNA 和 miRNA 数据，只保留共同样本。
2. 检查数据范围，并解释：
   - RNA 是否还需要 log 转换
   - miRNA 是否还需要 log 转换
3. 对 RNA 做特征筛选：
   - 去掉低表达/低方差特征
   - 给出合理阈值并解释
4. 对 miRNA 做特征筛选：
   - 先统计缺失情况
   - 先删高缺失 miRNA，再对剩余少量缺失做填充
   - 给出为什么不能一开始就全量填充
5. 对 RNA 和 miRNA 分别做标准化（如 z-score），解释原因。
6. 对 RNA 和 miRNA 分别做 PCA，输出：
   - 标准化后的矩阵
   - PCA 低维表示
   - 方差解释率图
7. 告诉我推荐保留多少个主成分给后续聚类。
8. 输出质量控制图和中间结果文件。

要求：
- miRNA 缺失很多，请你优先采用“过滤为主、填充为辅”的策略
- 代码完整可运行
- 最终给出明确结论：RNA 保留多少特征、miRNA 保留多少特征、各自保留多少 PCA 维度
- 文件命名清楚，方便窗口5直接读取
```

### 这个窗口的产出

* `rna_processed.csv`
* `mirna_processed.csv`
* `rna_pca.csv`
* `mirna_pca.csv`
* `rna_mirna_qc.png`

---

# 窗口3：CNV + 突变预处理

给 3 号同学

```text
现在只做 CNV 和突变数据预处理，不做聚类、不做临床分析。

背景：
- 使用窗口1导出的 master_samples_6omics.csv
- CNV 是 GISTIC thresholded 数据，数值通常为 -2,-1,0,1,2
- 突变数据是 0/1 二值矩阵
- 我当前上传的突变文件是部分数据，但我要按“未来可替换成全量文件”的规范来写流程

请你完成以下任务，并给出完整 Python 代码：

1. 读取 CNV 和突变数据，只保留共同样本。
2. 检查 CNV 数据分布，解释这种数据适不适合再做 log。
3. 对 CNV 做特征筛选：
   - 去掉低方差基因
   - 或者给出更合理的筛选策略
4. 对突变矩阵做特征筛选：
   - 去掉全 0 基因
   - 去掉突变频率过低的基因，并说明阈值
   - 如果当前文件基因数本来就少，请说明是否保留全部
5. 解释 CNV 和突变是否都需要标准化。
6. 为后续整合准备低维表示：
   - CNV 做 PCA
   - 突变根据当前维度选择 PCA / TruncatedSVD / 保留原始筛选后矩阵，并说明原因
7. 输出可供窗口5直接使用的低维矩阵和中间文件。
8. 如果以后替换成全量突变文件，你要顺手给我保留一个可复用流程，不要写死在当前 29 个基因上。

要求：
- 先解释数据类型差异：CNV 是离散有序值，突变是二值
- 不要把突变当连续表达数据乱处理
- 最后明确给出：CNV 保留多少特征、突变保留多少特征、各自低维表示多少维
```

### 创新方法A：mutation 通路/网络聚合后再融合

```text
【创新方法A：插入到窗口3的 mutation 分支中】

现在你仍然只做 CNV 和突变预处理，不做临床分析；但在“突变模态”里额外加入一个创新模块：先做 mutation 的通路/网络聚合，再把聚合后的低维表示送入后续多组学融合。

背景：
- 使用窗口1导出的 master_samples_6omics.csv
- 突变矩阵是 0/1 二值、稀疏、噪声大
- 当前突变文件是部分数据，但流程必须写成未来可替换全量文件的版本
- 本创新模块的目标不是推翻原流程，而是在 baseline mutation embedding 之外，增加一个更符合突变数据特性的表示版本

请你在原窗口3任务基础上，额外完成以下内容，并给出完整 Python 代码：

1. 先保留原始 baseline 流程：
   - 去全 0 基因
   - 去低频突变基因
   - 得到 baseline 的 mutation_processed.csv 和 mutation_embedding.csv
2. 额外新增“通路/网络聚合”分支：
   - 准备 pathway-gene 映射表，或用一个可替换的 pathway 字典结构
   - 将基因级 0/1 突变矩阵聚合为 sample × pathway 矩阵
   - 每个 pathway 给出合理的聚合方式，例如突变基因数、是否发生突变、或标准化后的 pathway mutation score
3. 如果当前没有完整通路文件，请你写成“接口可替换”的形式：
   - 先给出默认示例 pathway 集合结构
   - 同时说明以后换成 MSigDB / KEGG / Reactome 映射时应该替换哪里
4. 对 pathway-level mutation matrix 再做低维表示：
   - 优先考虑 TruncatedSVD 或 NMF
   - 给出为什么不建议直接把原始基因级 0/1 稀疏矩阵无脑拼进融合
5. 输出创新版结果文件：
   - mutation_pathway_matrix.csv
   - mutation_pathway_embedding.csv
   - mutation_pathway_qc.png
   - mutation_innovation_notes.md
6. 明确比较 baseline 与 innovation 两版的差异：
   - 原始 mutation embedding 的维度
   - pathway/network mutation embedding 的维度
   - 各自适合后续融合的原因
7. 代码不要写死在当前 29 个基因上，要保证以后替换成全量突变矩阵后仍能复用。

要求：
- 先解释为什么突变数据适合做通路/网络层表示，而不是直接按连续表达矩阵处理
- 创新版流程必须保留 baseline，方便窗口5/6/7做前后对比
- 最终明确告诉我：baseline 保留多少特征、创新版 pathway 保留多少特征、创新版低维表示保留多少维
```

### 创新方法B：CNV 的 NMF / 谱表示增强模块

```text
【创新方法B：插入到窗口3的 CNV 分支中】

现在你仍然只做 CNV 和突变预处理，不做临床分析；但在“CNV 模态”里额外加入一个创新模块：不用只做 PCA，而是增加一个更适合 CNV 结构的单模态表示方法，例如 NMF 或谱表示，再把这个新表示送入后续融合。

背景：
- 使用窗口1导出的 master_samples_6omics.csv
- CNV 是 GISTIC thresholded 数据，通常为 -2,-1,0,1,2
- CNV 不是普通连续表达量，直接 PCA 可以作为 baseline，但还可以尝试更有结构性的表示学习
- 本创新模块目标是增强 CNV 的聚类结构表达，而不是推翻原主线

请你在原窗口3任务基础上，额外完成以下内容，并给出完整 Python 代码：

1. 先保留原始 baseline 流程：
   - 保留共同样本
   - 去低方差基因
   - 做 baseline 的 CNV PCA
   - 输出 cnv_processed.csv 和 cnv_pca.csv
2. 额外新增 CNV 创新版分支：
   - 解释 NMF、谱表示、PCA 三者在 CNV 上的差异
   - 根据当前数据情况优先选一种方法实现，优先推荐 NMF；如果数据分布或非负性处理不方便，也可以说明并改用 spectral embedding
3. 如果采用 NMF，请说明：
   - 如何把 CNV 数据变换到适合 NMF 的形式
   - rank 应如何搜索，例如 10~30
   - 如何得到样本低维系数矩阵 W
4. 如果采用谱表示，请说明：
   - 如何构建样本相似度
   - 如何提取低维 embedding
5. 输出创新版结果文件：
   - cnv_nmf_embedding.csv 或 cnv_spectral_embedding.csv
   - cnv_nmf_qc.png 或 cnv_spectral_qc.png
   - cnv_nmf_vs_pca_comparison.xlsx
6. 明确比较 baseline 与 innovation 两版：
   - PCA 版维度
   - NMF / spectral 版维度
   - 哪种更适合作为窗口5的融合输入
7. 代码不要依赖当前文件的固定维度，要保证未来更换更完整 CNV 数据时仍然可复用。

要求：
- 先解释为什么 CNV 是离散有序矩阵，不能简单等同于普通表达矩阵
- 创新版流程必须与 baseline 并行保留，方便后续做前后比较
- 最终明确告诉我：baseline CNV 保留多少特征、创新版保留多少维、推荐窗口5优先接哪一版
```

### 这个窗口的产出

* `cnv_processed.csv`
* `mutation_processed.csv`
* `cnv_pca.csv`
* `mutation_embedding.csv`

---

# 窗口4：甲基化预处理

给 4 号同学

```text
现在只做甲基化数据预处理，不做聚类、不做临床分析。

背景：
- 使用窗口1导出的 master_samples_6omics.csv
- 甲基化数据是 beta value，范围通常在 0 到 1
- 我想按“完整高维甲基化数据”的思路设计流程
- 但当前上传文件是部分数据，所以请你写成“当前能跑通，未来可替换全量文件”的版本

请你基于甲基化文件完成以下任务，并给出完整 Python 代码：

1. 读取甲基化数据，只保留共同样本。
2. 检查探针数、缺失情况、数值范围。
3. 解释甲基化 beta 值是否需要 log 转换，是否适合直接 z-score。
4. 先删掉高缺失探针，再处理剩余少量缺失。
5. 如果探针数很高，应采用什么降维前筛选策略：
   - 高方差
   - MAD
   - Top N 探针
   - 为什么不能全量直接拼接进多组学聚类
6. 基于当前文件实际情况，给出你最终采用的策略。
7. 输出：
   - 清洗后的甲基化矩阵
   - 降维后的甲基化低维表示
   - 相关 QC 图
8. 同时补充一个“如果以后换成全量 450k 文件，应该怎么改参数”的说明。

要求：
- 重点讲清楚“高维甲基化的正确处理思路”
- 不要只针对当前 6 个探针写死
- 结果要能直接给窗口5用于整合聚类
```

### 这个窗口的产出

* `methylation_processed.csv`
* `methylation_embedding.csv`
* `methylation_qc.png`

---

# 窗口5：多组学整合与聚类

给 5 号同学

```text
现在只做多组学整合与聚类，不做临床和生存分析。

背景：
- 我已经有 RNA、miRNA、CNV、突变、甲基化、RPPA 的清洗后数据或低维表示
- 目标是建立一个稳健的多组学胃癌分型
- 不能把 clinical/survival 用进聚类
- 需要平衡方法复杂度和课程作业可完成性

请你完成以下任务，并给出完整 Python 代码：

1. 读取窗口2/3/4以及 RPPA 预处理后的低维矩阵，按共同样本对齐。
2. 解释为什么多组学不能直接把所有原始特征无脑拼接。
3. 给出最终融合策略，优先考虑“各组学先降维，再拼接低维表示”。
4. 对聚类数 k = 2,3,4,5,6 逐一尝试。
5. 聚类方法至少比较两种，例如：
   - KMeans
   - 层次聚类
   - 或带重采样的 consensus clustering 思路
6. 对每个 k 评估：
   - silhouette
   - cluster size 是否失衡
   - 重采样稳定性/重复聚类一致性
7. 选择一个最终最合理的 k，并给出理由。
8. 输出最终 cluster label 文件，以及聚类可视化图（PCA/UMAP/t-SNE 二维图均可，但注意解释）。
9. 告诉我最终各 cluster 的样本数。

要求：
- 不能只凭肉眼选 k
- 不能使用 clinical/survival 参与聚类
- 最终输出 cluster_labels.csv，供窗口6/7/8直接使用
- 先解释方案，再给完整代码
```

### 创新方法C：单模态聚类 + 共识融合

```text
【创新方法C：插入到窗口5主流程之前，作为融合策略升级版】

现在你只做多组学整合与聚类，不做临床和生存分析；但除了 baseline 的“各组学低维表示直接拼接再聚类”之外，请你额外实现一个创新版流程：先做单模态聚类或单模态相似度建模，再做共识融合，比较它与 baseline 的差异。

背景：
- 我已经有 RNA、miRNA、CNV、突变、甲基化、RPPA 的清洗后数据或低维表示
- 目标是建立一个稳健的多组学胃癌分型
- 老师强调“模态内各自处理”，所以这里希望把“单模态结构”显式保留下来，再做融合
- 创新模块不能引入 clinical/survival，只能基于组学本身完成

请你在原窗口5任务基础上，额外完成以下内容，并给出完整 Python 代码：

1. 先保留 baseline 流程：
   - 读取各组学低维表示
   - 直接拼接成 multiomics_embedding.csv
   - 对 k = 2,3,4,5,6 做聚类和评估
2. 新增“单模态聚类 + 共识融合”分支：
   - 每个模态单独构建 embedding 或相似度矩阵
   - 每个模态单独聚类，或得到样本-样本相似度
   - 再做 consensus clustering / similarity fusion / multi-view clustering 风格的整合
3. 明确说明你最终选用的共识融合实现路径：
   - 如果做单模态 cluster label 融合，说明如何形成共识矩阵
   - 如果做 similarity matrix 融合，说明如何融合不同模态相似度
4. 对 baseline 与 innovation 两版都评估：
   - silhouette
   - cluster size 是否失衡
   - 重采样稳定性
   - 不同随机种子的一致性
5. 输出两版结果并做比较：
   - baseline_cluster_labels.csv
   - innovation_consensus_cluster_labels.csv
   - baseline_vs_consensus_comparison.xlsx
   - cluster_visualization.png
6. 如果创新版效果不如 baseline，也要如实说明，并给出为什么课程作业最终仍可能优先保留 baseline 主结果。
7. 最终明确推荐：
   - 哪一版作为主分析结果
   - 哪一版作为创新增强结果
   - 推荐的最终 k 是多少，理由是什么

要求：
- 先解释为什么多组学不能只看“直接拼接”，以及为什么“单模态先建结构再融合”更有方法层次
- 不能使用 clinical/survival 参与任何聚类或选 k
- 输出文件命名清楚，方便窗口6/7直接接收 baseline 与 innovation 两版标签做比较
```

### 这个窗口的产出

* `multiomics_embedding.csv`
* `cluster_labels.csv`
* `cluster_visualization.png`

---

# 窗口6：鲁棒性评估与亚型特征解释

给 6 号同学

```text
现在只做“聚类结果的鲁棒性评估”和“亚型特征解释”，不做临床生存分析，不做监督模型。

背景：
- 我已经有窗口5输出的最终 cluster_labels.csv
- 课程要求强调鲁棒性
- 我需要在报告里说明：这个分型不是偶然的，而且各亚型在组学层面有可解释特征

请你完成以下任务，并给出完整 Python 代码：

1. 基于窗口5的聚类结果，做聚类鲁棒性评估：
   - 不同随机种子是否稳定
   - 重采样后标签一致性如何
   - 换一个聚类算法结果是否大体一致
2. 生成可以写进报告的鲁棒性图或表。
3. 对每个 cluster 做组学特征解释：
   - RNA 层找代表性高/低表达特征
   - CNV 层找代表性扩增/缺失特征
   - 突变层找 cluster 富集基因
   - 甲基化层找代表性探针
   - RPPA 层找代表性蛋白
4. 尽量输出一个综合热图或分组特征图，方便 PPT 展示。
5. 帮我总结每个 cluster 的“标签式描述”，例如：
   - cluster 1：高甲基化/低蛋白活性/某些基因高表达
   - cluster 2：CNV 活跃/某类突变富集
6. 注意不要过度解释，要基于实际数据结果写。

要求：
- 重点是“为什么这个聚类鲁棒”
- 输出的图和表要适合直接放进实验报告
- 最终给出每个 cluster 的一句话特征总结
```

### 这个窗口的产出

* `cluster_robustness_report.xlsx`
* `cluster_feature_summary.xlsx`
* `heatmap_subtypes.png`

---

# 窗口7：临床与生存验证

给 7 号同学

```text
现在只做 cluster 与 clinical/survival 的后验验证，不参与聚类，不参与建模。

背景：
- 已有 cluster_labels.csv
- 临床文件和生存文件中包含胃癌样本的表型与随访信息
- survival 中有 OS、DSS、DFI、PFI 终点
- 目标是验证这个分型是否有临床意义

请你基于 cluster_labels.csv、clinicalMatrix、survival 文件，完成以下任务，并给出完整 Python 代码（pandas/scipy/lifelines）：

1. 将 cluster 标签与 clinical、survival 数据按样本对齐。
2. 检查 clinical 中可用变量，挑选最适合做分型验证的变量，例如：
   - 年龄
   - 性别
   - 分期
   - 分级
   - 肿瘤部位
   - 其他缺失较少且有意义的变量
3. 对不同 cluster 做临床差异检验：
   - 分类变量：卡方或 Fisher
   - 连续变量：Kruskal-Wallis 或 ANOVA
4. 对 survival 做生存分析：
   - 至少做 OS
   - 尽量补做 PFI
   - 视缺失情况决定是否做 DSS / DFI
5. 绘制 Kaplan-Meier 曲线，做 log-rank 检验。
6. 如样本量允许，进一步做 Cox 回归，可加入年龄/分期作为协变量。
7. 帮我解释结果：哪些 cluster 预后更差，哪些临床特征和 cluster 相关。
8. 输出适合报告使用的图表和结论。

要求：
- clinical/survival 只能用于后验验证，不能反哺聚类
- 先检查缺失，再决定用哪些变量
- 最终明确告诉我：这个分型是否具有临床意义、证据是什么
```

### 这个窗口的产出

* `clinical_cluster_association.xlsx`
* `km_os.png`
* `km_pfi.png`
* `cox_results.xlsx`

---

# 窗口8：监督分类模型 + 报告/PPT 汇总

给 8 号同学

```text
现在只做最终分类模型和报告收尾，不重新做聚类。

背景：
- cluster_labels.csv 已经确定
- 现在要把 cluster 当成监督学习标签，建立一个能给新样本判型的分类器雏形
- 同时我需要把全项目结果整理成实验报告和 PPT

请你完成以下任务，并给出完整 Python 代码和写作框架：

第一部分：分类模型
1. 读取已经处理好的多组学特征或融合特征，以及 cluster_labels.csv。
2. 设计监督分类任务：输入多组学特征，输出 cluster 类别。
3. 选择合适模型并比较，例如：
   - Random Forest
   - SVM
   - XGBoost（如果环境允许）
4. 做交叉验证或训练/测试划分。
5. 输出评价指标：
   - accuracy
   - macro F1
   - confusion matrix
   - 如适合可加 ROC/AUC
6. 给出一个最适合作业展示的最终模型，并解释原因。
7. 输出特征重要性或可解释性结果（若模型支持）。

第二部分：报告/PPT汇总
8. 按“背景-数据-预处理-聚类-鲁棒性-临床验证-分类模型-讨论-分工”的结构，给我一版完整提纲。
9. 帮我整理所有窗口的图表应该放在哪一节。
10. 帮我生成“8个人项目分工和贡献度说明”的草案。
11. 帮我列出最后最关键的结论 5-8 条，适合答辩或汇报。

要求：
- 建模阶段不能信息泄漏
- 先做特征筛选/降维，再建模
- 模型要以“可解释、可交差、能展示”为优先
- 最终给出一版可直接写进 Word/PPT 的框架
```

### 这个窗口的产出

* `classifier_metrics.xlsx`
* `confusion_matrix.png`
* `feature_importance.png`
* `report_outline.md`
* `team_contribution_draft.xlsx`

---

## 你们 8 个人怎么配合

最重要的是这三条：

### 1. 只认窗口1导出的共同样本表

后面所有人都别自己重新随手取交集，不然最后样本对不上。

### 2. 聚类前只碰组学，别碰 clinical/survival

不然第7窗口的临床显著性会“虚高”。

### 3. 第4窗口和第3窗口都要按“可替换全量文件”写

因为你现在的甲基化和突变是部分数据。
这样你们先把作业跑通；万一后面补到全量文件，只需要替换局部，不用推翻重来。

---

## 我替你定一个最实用的贡献度草案

先给你一个可用版本，后面窗口8还能再润色：

* 1号：总控、样本对齐、数据字典、流程规范，**15%**
* 2号：RNA/miRNA 预处理与降维，**13%**
* 3号：CNV/突变预处理与降维，**13%**
* 4号：甲基化预处理与降维，**13%**
* 5号：多组学整合聚类与选 k，**16%**
* 6号：鲁棒性评估与亚型特征解释，**10%**
* 7号：临床与生存验证，**10%**
* 8号：分类模型、报告整合、PPT 汇总，**10%**

如果组长还负责最后统稿，可以把 1 号调到 **18%**，从 6/7/8 里各扣一点。

---

## 最后给你一句总策略

**你们现在不要纠结“当前甲基化只有 6 个探针、突变只有 29 个基因还能不能做”。答案是：能做，而且应该先把完整流程跑通。**
你现在最需要的是一个 **可复用、可替换、可解释、能交作业** 的六组学管线，而不是一开始就把自己卡死在“必须等最完美数据”。

下一步你直接把上面这 8 段提示词分别丢进 8 个窗口，先从窗口1开始。
